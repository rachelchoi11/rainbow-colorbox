
# 100MB 를 넘는 파일은 GitHub 이 거부한다. 실제로 8/10 부터 3주간 푸시가 막혀 있었다.
# 큰 원본은 저장소에 두지 않고 별도 보관한다(~/workspace/work-dashboard-대용량백업 등).
*.zip
*.bundle
hivemind/analysis_2026/output/records.json

# ⚠️ 이 레포의 최상위는 ~/workspace 다. work-dashboard 안에서 `git add -A` 를 해도
#    여기까지 딸려 온다 — 실제로 9/1 에 824MB 번들이 커밋에 섞여 푸시가 막혔다.
#    폴더 «자체»를 막아 둔다. 안의 파일은 추출한 대용량 blob 의 유일한 사본이라 지우지 않는다.
work-dashboard-대용량백업/
