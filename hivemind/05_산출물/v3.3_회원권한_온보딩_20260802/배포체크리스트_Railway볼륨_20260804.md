# 배포 체크리스트 — Railway 볼륨 정식 배포 (2026-08-04)

> ⚠️ 순서 중요: **볼륨을 먼저 붙인 뒤 배포**해야 데이터·계정이 유지됩니다. 볼륨 없이 배포하면 재배포마다 SQLite(app.db)·세션·계정이 소멸(빈 앱=최초 가입 부트스트랩).

## 0. 사전 확인
- 코드: feat/parser-1112-yes24-kakao (main 대비 21커밋). Dockerfile 기반, uvicorn `${PORT}`.
- 저장 경로: 모든 상태가 `/app/data` 아래(app.db·.session_secret·contract_files·label_publisher_map.json).

## 1. Railway — 볼륨 + 환경변수 (프로젝트: lively-dream, 대시보드에서 먼저)
> ⚠️ 마운트는 **`/app/appdata`** (❌ `/app/data` 아님 — data엔 이미지에 포함된 샘플·매핑표가 있어 가려짐).
1. lively-dream → 서비스 클릭 → **Settings → Volumes → New Volume**, Mount path = **`/app/appdata`**.
2. **Variables**:
   - `SESSION_SECRET` = `b656450bb9994479b9fc32c7440815d96aa89e6cfc199ddf2857adc3a251d711` (고정)
   - `ALADIN_KEY` = (알라딘 API 키 — 기존 값 재사용)
   - `APP_DB` = `/app/appdata/app.db`
   - `CONTRACT_DIR` = `/app/appdata/contract_files` (계약서 첨부 영속)
   - `PORT` = Railway 자동(건드리지 않음)
3. Settings → Source → 배포 브랜치 = **main** 확인. 빌드 = Dockerfile 자동.
> 볼륨 위: SQLite(app.db=출판사·계약·회원·정산런·설정) + 계약서 첨부. 샘플·매핑표는 이미지에서 읽음(정상).

## 2. 배포 (볼륨 설정 후)
- main 브랜치 자동배포면: `feat/parser-1112-yes24-kakao` → `main` 머지 후 push → 자동 빌드.
  ```
  git checkout main && git merge --no-ff feat/parser-1112-yes24-kakao && git push origin main
  ```
- 또는 Railway가 feat 브랜치를 바라보게 설정.

## 3. 최초 1회 시드 (배포 후, 볼륨에 영속)
빈 볼륨이므로 데이터가 없음 → 리뷰 시연용 데이터 세팅(한 번만, 볼륨에 남음):
1. 배포 URL `/register` → 최초 운영사 계정 생성(자동 최고관리자·활성). ← **회의 시연 계정**
2. 로그인 → 정산서 업로드(또는 샘플 로드) → **출판사 관리 > 정산 데이터에서 출판사 등록** → 128 출판사 시드.
3. **계약사 계정 발급**(스토린랩 등)으로 계약사 데모 계정 1개 생성 → 동의·온보딩까지.
4. (선택) 매핑표(label_publisher_map.json)는 이미지에 포함되어 자동 반영.

## 4. 배포 후 점검
- `/healthz` 200, `/login` 정상, 로그인→테넌트 목록, 성과 분석·정산 마감 화면.
- 재배포 1회 후에도 계정·데이터 유지되는지(볼륨 확인).

## 주의
- 기존 v2 프로덕션 데이터는 JSON(gitignore)라 이미 비영속 — 새 배포로 대체되어도 손실 이슈 없음.
- 민감정보(users.json 등)는 리포 미포함(gitignore) → 계정은 배포 후 생성.
- 로그인 시도 제한(레이트리밋)은 아직 없음 → 공개 URL이면 곧 추가 권장.
