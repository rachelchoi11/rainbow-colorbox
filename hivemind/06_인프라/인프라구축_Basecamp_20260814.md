# 하이브마인드 — 인프라 구축 프로젝트 (Basecamp)

> 작성 2026-08-14 · PM/오너 최혜윤(Rachel) · 실행 강수아(suahkang)
> 이 문서는 Basecamp의 **Message(개요)** + **To-do 리스트(할 일)** 로 옮겨 쓰도록 구성했습니다.

---

## 📌 Message Board — 프로젝트 개요 (그대로 붙여넣기)

**목표:** 지금까지 Rachel이 임시로 운영해 온 정산 솔루션을, 출판사에 정식 오픈 가능한 **운영급 인프라**로 세우고, 인프라 운영을 강수아에게 이관한다.

**현재 상태:**
- 앱: FastAPI(Python) + 자체 프론트(바닐라 JS) + SQLite(Railway 볼륨). 라이브 배포 중, 매일 진화 중.
- 리포: github.com/rachelchoi11/hivemind (강수아 협업자 등록 완료).

**원칙:**
1. **스택은 Python 유지** — 재작성 안 함. 핵심 자산(정산서 28종 파서·40만 행 처리)은 pandas(Python)가 최적이고, 이 규모의 B2B에선 서버 성능이 병목이 아님. 성능 벽이 실제로 오면 그때 뜨거운 부분만 최적화(파이프라인은 Python 유지).
2. **결정은 Rachel, 실행은 강수아.** 블랙박스 금지 — 모든 변경은 PR + 무엇을/왜 설명 + Rachel 승인.
3. **출시 전 필수부터.** 나머지는 백로그.

**협업 도구:** 이 Basecamp로 일원화(기존 디스코드 대체). 할 일·결정·논의는 여기서.

---

## ✅ To-do 리스트 1 — [먼저] Rachel 결정 (구현 전 선행)

> 아래 5개가 정해져야 강수아가 실행에 들어감. 각 항목에 권장안 표기.

- [ ] **스택 유지 확정** — Python 유지(권장) vs 재작성. → 권장: 유지. `담당: Rachel`
- [ ] **호스팅 확정** — Railway 유지(권장) / Render·Fly / AWS. → 권장: Railway 유지(관리 쉬움). `담당: Rachel`
- [ ] **DB 이전 시점** — 정식 오픈 전 Postgres 이전(권장). → 권장: 오픈 전 실행. `담당: Rachel`
- [ ] **도메인 결정** — `app.hive-mind.kr`(권장, 기존 이메일 도메인 재사용) vs 신규. `담당: Rachel`
- [ ] **이메일 서비스 선택** — Postmark/SendGrid(연동 쉬움, 권장) / AWS SES(저렴·설정 복잡). `담당: Rachel`

---

## ✅ To-do 리스트 2 — 도메인 · 접속

- [ ] 도메인 구입/확인 (`hive-mind.kr` 보유 시 서브도메인 사용) `담당: 강수아`
- [ ] DNS에서 `app.hive-mind.kr` → Railway 연결(CNAME) `담당: 강수아`
- [ ] HTTPS 인증서 자동 적용 확인(Railway 자동) `담당: 강수아`
- **완료조건:** `https://app.hive-mind.kr` 로 정상 접속·자물쇠(HTTPS) 확인.

---

## ✅ To-do 리스트 3 — 데이터베이스 (SQLite → Postgres)

> 설계 문서 있음: `내부_Rachel/DB이전설계_SQLite→Postgres_20260806.md` (db.py가 KV 추상화라 백엔드 교체만).

- [ ] Railway에 **Postgres 플러그인** 추가 → `DATABASE_URL` 발급 `담당: 강수아`
- [ ] `db.py`에 Postgres 백엔드 추가(현 read/write 인터페이스 유지, `DATABASE_URL` 있으면 Postgres) `담당: 강수아`
- [ ] 기존 `app.db`(SQLite) → Postgres **1회 이관 스크립트**(컬렉션 1:1 복사) `담당: 강수아`
- [ ] 이관 검증: 회원·테넌트(128)·계약·정산런 카운트 대조 + 회귀 스크립트 통과 `담당: 강수아` → `검토: Rachel`
- **완료조건:** 재배포·검증 후 회원/계약/정산 데이터 정상, 롤백 경로(SQLite 스냅샷) 보관.

---

## ✅ To-do 리스트 4 — 이메일 발송 (초대·알림)

> 현재 초대는 링크 수동 전달. 정식 오픈엔 자동 발송 필요.

- [ ] 이메일 서비스 계정 생성(선택안) + 발신 도메인 인증(SPF/DKIM) `담당: 강수아`
- [ ] 앱에 SMTP/API 연동 — 계약사 초대 메일 자동 발송 `담당: 강수아`
- [ ] (후속) 정산 완료·마감 리마인더 알림 메일 `담당: 강수아`
- **완료조건:** 초대 발급 시 실제 메일 수신 확인, 스팸함 아님.

---

## ✅ To-do 리스트 5 — 보안 · 안정성

- [ ] **로그인 시도 제한(레이트리밋)** 추가 — 공개 URL 무차별 대입 방어 `담당: 강수아`
- [ ] 시크릿(세션키·API키) 전부 환경변수 확인, 코드/리포 미포함 재점검 `담당: 강수아`
- [ ] 백업 점검 — DB 자동 백업(구현됨) + Postgres 관리형 백업 확인 `담당: 강수아`
- [ ] (권장) 취약점 1차 점검(권한·테넌트 격리 재확인) `담당: 강수아` → `검토: Rachel`
- **완료조건:** 레이트리밋 동작, 시크릿 노출 0, 백업 복구 테스트 1회.

---

## ✅ To-do 리스트 6 — 배포 · 협업 흐름

- [ ] **main 브랜치 보호** + **PR 필수**(강수아 push 대비, 리뷰 강제) `담당: Rachel(설정)`
- [ ] **스테이징(dev) 환경 분리** — dev 배포 ↔ prod 배포 별도 `담당: 강수아`
- [ ] 배포 절차 문서화(누가·어떻게 배포하는지) `담당: 강수아`
- **완료조건:** main 직접 push 차단, PR→리뷰→머지→자동배포 흐름 확립, dev에서 먼저 검증.

---

## ✅ To-do 리스트 7 — 모니터링 · 에러추적

- [ ] **에러 추적**(Sentry 등) 연동 — 장애 즉시 인지 `담당: 강수아`
- [ ] **업타임 모니터**(다운 시 알림) `담당: 강수아`
- **완료조건:** 앱 에러/다운 발생 시 알림 수신.

---

## 🗂️ 백로그 (나중 · 성장하면)

- 멀티 인스턴스 확장(수평 확장) · 오브젝트 스토리지(S3) 전환
- 세무·지급 연동(팝빌 등) · 회계 연동
- (선택) 프론트 Next.js 전환 — 백엔드 엔진은 Python 유지. **출시 후 검토.**
- B4 정산 세션 영속(업로드 정산 데이터 재배포 생존) — 제품 백로그와 연동

---

## 🔑 요약 — 지금 Rachel이 정할 5가지
| 결정 | 권장 |
|---|---|
| 스택 | Python 유지 |
| 호스팅 | Railway 유지 |
| DB | 오픈 전 Postgres 이전 |
| 도메인 | app.hive-mind.kr |
| 이메일 | Postmark/SendGrid |

이 5개 확정 → 강수아가 리스트 2~7 실행 착수.
